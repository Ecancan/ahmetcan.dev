// experimental

// import { useQuery } from 'react-query';
//
// export default function useReactQuery(props) {
//   const { queryFn, key, options } = props || {};
//
//   const result = useQuery(key, queryFn, options);
//
//   return result;
// }
