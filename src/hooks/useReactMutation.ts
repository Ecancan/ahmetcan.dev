// experimental

// import { useMutation } from "react-query";
//
// export default function useReactMutation(props) {
//  const { queryFn, key, options } = props || {};
//
//  const result = useMutation(key, queryFn, options);
//
//  return result;
// }
